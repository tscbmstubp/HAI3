## 1. CLI Template Pipeline

- [x] 1.1 Add `src/app/mfe` to `packages/cli/template-sources/manifest.yaml` directories list.
- [-] 1.2 ~~Add `registry.ts` — MFE registry with enabled/port fields.~~ **Dropped** (PR review C1: registry.ts architecturally redundant — never consumed at runtime. Replaced by direct `package.json` scanning in `dev-all.ts`.)
- [x] 1.3 Add `packages/cli/template-sources/project/src/app/mfe/bootstrap.ts` — dynamic multi-MFE bootstrap that reads from `generated-mfe-manifests`, with empty-registry guard.
- [x] 1.4 Add `packages/cli/template-sources/project/src/app/mfe/generated-mfe-manifests.ts` — empty stub (regenerated by `generate:mfe-manifests` script).
- [x] 1.5 Add `packages/cli/template-sources/project/scripts/dev-all.ts` — scans `src/mfe_packages/*/package.json` for port (via `--port` in preview/dev script), spawns MFE servers + main app via concurrently.
- [x] 1.6 Add `packages/cli/template-sources/project/scripts/generate-mfe-manifests.ts` — scans `src/mfe_packages/*/mfe.json`, writes typed imports. Handles missing directory gracefully.
- [x] 1.7 Add `packages/cli/template-sources/mfe-package/package.json` and `vite.config.ts` — template for `hai3 add-mfe`.

## 2. Project Generator

- [x] 2.1 Update `packages/cli/src/generators/project.ts`: add `generate:mfe-manifests` and `dev:all` scripts to generated `package.json`; prepend `npm run generate:mfe-manifests &&` to `dev`, `build`, `type-check`.
- [x] 2.2 Remove dead screensets copy loop from `project.ts` (referenced non-existent `src/screensets/` directories).
- [x] 2.3 Remove stale template overrides from `packages/cli/scripts/copy-templates.ts`.

## 3. Terminology Fix

- [x] 3.1 Update `packages/cli/src/commands/create/index.ts`: replace `hai3 screenset create` with `hai3 add-mfe` in post-create message; fix log text.
- [x] 3.2 Update all 4 App.tsx variants in `template-sources/project/src/app/`: replace `hai3 screenset create` with `hai3 add-mfe`.

## 4. Monorepo App Update

- [-] 4.1 ~~Add `src/app/mfe/registry.ts` with demo-mfe entry.~~ **Dropped + reversed** (PR review C1: registry.ts deleted from monorepo via `git rm`.)
- [x] 4.2 `src/app/mfe/generated-mfe-manifests.ts` — now gitignored, regenerated by `generate:mfe-manifests`. Removed from git index via `git rm --cached`.
- [x] 4.3 Rewrite `src/app/mfe/bootstrap.ts` — dynamic: reads `MFE_MANIFESTS` from `./generated-mfe-manifests`, loops dynamically, guards for empty registry and empty screenExtensions array.
- [x] 4.4 Rewrite `scripts/dev-all.ts` — scans `src/mfe_packages/*/package.json` directly (PR review W2: removed fragile registry.ts regex parsing).
- [x] 4.5 `package.json` has `generate:mfe-manifests`, `dev:all`; MFE scripts prepended to `dev`, `build`, `type-check`.

## 5. Documentation

- [x] 5.1 Add `.ai/GUIDELINES.hai3-mfe-setup.md`.
- [x] 5.2 Add `.ai/commands/user/hai3-new-mfe.md`.
- [x] 5.3 Add `.ai/commands/user/hai3-dev-all.md`.
- [x] 5.4 Add `.ai/commands/user/hai3-add-mfe-to-registry.md`.
- [x] 5.5 Remove all `user-dashboard-mfe` references from documentation.

## 6. PR Review Fixes (this session)

- [x] 6.1 Fix C3/W1: Update `.ai/GUIDELINES.md` dep invariant — standalone projects must declare L1/L2 peer deps explicitly.
- [x] 6.2 Fix C1+W2: Delete `src/app/mfe/registry.ts`; rewrite `scripts/dev-all.ts` to scan `src/mfe_packages/*/package.json` for port.
- [x] 6.3 Fix C2: Rewrite `src/app/mfe/bootstrap.ts` — imports `MFE_MANIFESTS` from `./generated-mfe-manifests` dynamically; guards for empty array; fixes `screenExtensions[0]` crash.
- [x] 6.4 Fix W3: Add `src/app/mfe/generated-mfe-manifests.ts` to `.gitignore`; remove from git index.
- [x] 6.5 Add `packages/react/commands/hai3-new-mfe.md` — bundled into generated projects via `copy-templates.ts`.
- [x] 6.6 Add monorepo IDE adapters: `.claude/commands/hai3-new-mfe.md`, `.cursor/commands/hai3-new-mfe.md`, `.windsurf/workflows/hai3-new-mfe.md`.

## 7. Verification

- [ ] 7.1 Run `npm run build --workspace=@hai3/cli` — CLI builds without errors.
- [ ] 7.2 Run `npm run arch:check` — architecture validation passes.
- [ ] 7.3 Verify `hai3 create` generates project with `src/app/mfe/` directory and MFE scripts.
